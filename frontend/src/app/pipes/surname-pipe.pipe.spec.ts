import { SurnamePipePipe } from './surname-pipe.pipe';

describe('SurnamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SurnamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
