import { CountryPipePipe } from './country-pipe.pipe';

describe('CountryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
